{
  "name": "server",
  "scripts": {
    "dev": "bun run --hot --no-clear-screen src/simple.ts",
    "dev:wrangler": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@openauthjs/openauth": "^0.4.3",
    "hono": "^4.7.11",
    "l1-env": "workspace:*",
    "l1-sync": "workspace:*",
    "l1-db": "workspace:*",
    "nanoid": "^5.1.5"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
